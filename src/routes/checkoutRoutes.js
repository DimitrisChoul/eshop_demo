const express = require("express");
const { checkout } = require("../controllers/checkoutController");
const router = express.Router();

// router.post("/:clientId", checkout);

module.exports = router;
